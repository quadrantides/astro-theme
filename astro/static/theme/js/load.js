var current_selection;

function save_new_selection(selection) {
   current_selection = selection
}

function load_transcend(graphDiv, aspectsLegendGraphDiv, aspectPlanetsLegendGraphDiv) {

    set_birthday_date(current_selection);
    // add_map(current_selection);
}
